@model IEnumerable<TPIntegradorBack.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>ABM de usuarios</h1>

<div>
    <a class="btn btn-success" asp-action="Create">Crear Nuevo Usuario</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreUsuario)
            </th>
@*             <th>
                @Html.DisplayNameFor(model => model.Contrasenia)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
@*             <th>
                @Html.DisplayNameFor(model => model.Pedidos)
            </th> *@
            <th>Pedidos</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.NombreUsuario)
            </td>
@*             <td>
                @Html.DisplayFor(model => item.Contrasenia)
            </td> *@
            <td>
                @Html.DisplayFor(model => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(model => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(model => item.Role)
            </td>
            <td>
                @if (item.Pedidos != null && item.Pedidos.Any())
                {
                    <ul class="list-unstyled mb-0">
                        @foreach (var pedido in item.Pedidos)
                        {
                            <li><span>Pedido N°@pedido.PedidoId</span> - $@pedido.MontoTotal</li>
                        }
                    </ul>
                }
                else
                {
                    <span class="text-muted">Sin pedidos</span>
                }
            </td>
            <td>
                <a class="btn btn-primary btn-sm" asp-action="Edit" asp-route-id="@item.UsuarioId">Editar</a> 
                <a class="btn btn-warning btn-sm" asp-action="Details" asp-route-id="@item.UsuarioId">Ver</a> 
                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.UsuarioId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
